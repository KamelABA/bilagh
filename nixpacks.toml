# Let Nixpacks use its default Python provider which automatically
# sets up a virtual environment (/opt/venv) to avoid PEP 668 errors.

[start]
cmd = "cd backend && python3 startup_download_models.py && uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
